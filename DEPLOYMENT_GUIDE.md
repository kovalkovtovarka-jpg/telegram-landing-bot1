# 🚀 Простая инструкция по деплою (для начинающих)

Пошаговая инструкция с точными указаниями, куда нажимать.

---

## 📋 Что нужно перед началом

1. ✅ Аккаунт на GitHub (если нет - зарегистрируйтесь на github.com)
2. ✅ Аккаунт на Railway (если нет - зарегистрируйтесь на railway.app)
3. ✅ Telegram Bot Token (получите от @BotFather в Telegram)
4. ✅ API ключ OpenAI (получите на platform.openai.com)

---

## ШАГ 1: Подготовка кода на GitHub

### 1.1. Откройте GitHub

1. Зайдите на сайт: **github.com**
2. Войдите в свой аккаунт (или зарегистрируйтесь)

### 1.2. Создайте новый репозиторий

1. В правом верхнем углу нажмите на **плюсик (+)** 
2. В выпадающем меню выберите **"New repository"**
3. Заполните:
   - **Repository name**: `telegram-landing-bot` (или любое имя)
   - **Description**: `Telegram bot for landing generation` (опционально)
   - **Public** или **Private** - выберите что хотите
4. **НЕ** ставьте галочки на "Add README", "Add .gitignore" и т.д.
5. Нажмите зеленую кнопку **"Create repository"**

### 1.3. Загрузите код на GitHub

Можно выбрать любой способ — результат один и тот же.

**Вариант А: Через браузер**

В текущем интерфейсе GitHub при **"Upload files"** нет ни кнопок, ни полей, куда можно вписать название папки или путь. Все загруженные файлы попадают только в корень репозитория. Загрузить структуру папок (backend, .github и т.д.) через «Upload files» нельзя.

Через браузер можно только:
- загрузить **файлы из корня** проекта (main.py, webhook.py, requirements.txt, README.md и т.д.) — их перетащите в область загрузки и нажмите **"Commit changes"**;
- либо создавать папки вручную через **"Add file"** → **"Create new file"**: в поле **"Name your file"** вверху страницы введите путь, например `backend/config.py` — тогда создастся папка `backend` и файл в ней. Но тогда содержимое каждого файла нужно вставлять вручную (скопировать из своего файла и вставить в форму). Для десятков файлов это неудобно.

**Вывод:** для этого проекта (с папками backend, .github и т.д.) загрузка только через браузер практически не подходит. Лучше сразу использовать **Вариант Б (GitHub Desktop)** или **Вариант В (командная строка)** — там папки и все файлы загружаются автоматически, без ручного ввода путей.

**Вариант Б: Через GitHub Desktop**

1. Скачайте GitHub Desktop: **desktop.github.com**
2. Установите и откройте
3. Нажмите **"File"** → **"Add Local Repository"**
4. Нажмите **"Choose..."**
5. Выберите папку вашего проекта: `D:\Сайты\creativelandingbotfixed`
6. Нажмите **"Add repository"**
7. Внизу в поле "Summary" напишите: `Initial commit`
8. Нажмите **"Commit to main"** (синяя кнопка внизу)
9. Нажмите **"Publish repository"** (кнопка справа вверху)
10. Выберите ваш репозиторий из списка
11. Нажмите **"Publish repository"**

**Вариант В: Через командную строку**

Откройте командную строку в папке проекта и выполните:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/telegram-landing-bot.git
git push -u origin main
```

(Замените `ВАШ_ЛОГИН` на ваш логин GitHub)

---

## ШАГ 2: Создание проекта на Railway

### 2.1. Войдите в Railway

1. Зайдите на сайт: **railway.app**
2. Нажмите **"Login"** (в правом верхнем углу)
3. Выберите **"Login with GitHub"**
4. Разрешите Railway доступ к вашему GitHub аккаунту

### 2.2. Создайте новый проект

1. На главной странице Railway нажмите большую зеленую кнопку **"New Project"**
2. В появившемся окне выберите **"Deploy from GitHub repo"**
3. Railway покажет список ваших репозиториев
4. Найдите и нажмите на **"telegram-landing-bot"** (или как вы назвали)
5. Railway начнет автоматически создавать проект

### 2.3. Дождитесь создания проекта

1. Вы увидите экран с процессом создания
2. Подождите 1-2 минуты
3. Railway автоматически:
   - Определит что это Python проект
   - Установит зависимости
   - Попытается запустить (но пока не запустится, т.к. нет переменных)

---

## ШАГ 3: Настройка переменных окружения

### 3.1. Откройте настройки сервиса

1. В Railway Dashboard вы увидите ваш проект
2. Нажмите на **сервис** (обычно называется как ваш репозиторий)
3. Вверху вы увидите вкладки: **"Deployments"**, **"Metrics"**, **"Variables"**, **"Settings"**
4. Нажмите на вкладку **"Variables"**

### 3.2. Добавьте Telegram Bot Token

1. Нажмите кнопку **"New Variable"** (справа вверху)
2. В поле **"Name"** введите: `TELEGRAM_BOT_TOKEN`
3. В поле **"Value"** вставьте ваш токен от @BotFather
4. Нажмите **"Add"** (или просто Enter)

**Как получить токен:**
1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3.3. Добавьте OpenAI API Key

1. Снова нажмите **"New Variable"**
2. В поле **"Name"** введите: `OPENAI_API_KEY`
3. В поле **"Value"** вставьте ваш OpenAI API ключ
4. Нажмите **"Add"**

**Как получить OpenAI API ключ:**
1. Зайдите на **platform.openai.com**
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите на ваш профиль (правый верхний угол)
4. Выберите **"API keys"**
5. Нажмите **"Create new secret key"**
6. Скопируйте ключ (начинается с `sk-`)

### 3.4. Добавьте остальные переменные

Нажмите **"New Variable"** для каждой:

**Переменная 1:**
- **Name:** `LLM_PROVIDER`
- **Value:** `openai`
- Нажмите **"Add"**

**Переменная 2:**
- **Name:** `LLM_MODEL`
- **Value:** `gpt-4o`
- Нажмите **"Add"**

**Переменная 3:**
- **Name:** `LLM_TEMPERATURE`
- **Value:** `0.3`
- Нажмите **"Add"**

**Переменная 4:**
- **Name:** `LLM_MAX_TOKENS`
- **Value:** `8000`
- Нажмите **"Add"**

### 3.5. Проверьте что все добавлено

В списке переменных должны быть:
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `OPENAI_API_KEY`
- ✅ `LLM_PROVIDER`
- ✅ `LLM_MODEL`
- ✅ `LLM_TEMPERATURE`
- ✅ `LLM_MAX_TOKENS`

---

## ШАГ 4: Создание публичного домена

### 4.1. Откройте настройки Networking

1. В Railway Dashboard откройте ваш сервис
2. Нажмите на вкладку **"Settings"** (вверху)
3. Прокрутите вниз до раздела **"Networking"**

### 4.2. Сгенерируйте домен

1. В разделе **"Networking"** найдите секцию **"Public Networking"**
2. Нажмите кнопку **"Generate Domain"** (фиолетовая кнопка с иконкой молнии)
3. Railway создаст домен автоматически
4. Домен будет вида: `ваш-проект-production.up.railway.app`
5. **Скопируйте этот URL** (он понадобится на следующем шаге)

**Пример:** `https://creativelandingbot-production-0adf.up.railway.app`

### 4.3. Добавьте WEBHOOK_URL

1. Вернитесь на вкладку **"Variables"**
2. Нажмите **"New Variable"**
3. В поле **"Name"** введите: `WEBHOOK_URL`
4. В поле **"Value"** вставьте скопированный URL (из шага 4.2)
5. **ВАЖНО:** URL должен быть БЕЗ слеша в конце!
   - ✅ Правильно: `https://creativelandingbot-production-0adf.up.railway.app`
   - ❌ Неправильно: `https://creativelandingbot-production-0adf.up.railway.app/`
6. Нажмите **"Add"**

### 4.4. Дождитесь перезапуска

После добавления `WEBHOOK_URL` Railway автоматически перезапустит приложение. Подождите 1-2 минуты.

---

## ШАГ 5: Проверка что все работает

### 5.1. Проверьте логи

1. В Railway Dashboard откройте ваш сервис
2. Нажмите на вкладку **"Deployments"** (вверху)
3. Вы увидите список деплоев
4. Нажмите на **последний деплой** (самый верхний)
5. Нажмите кнопку **"View Logs"** (или просто откройте логи)
6. Прокрутите вниз до конца логов
7. Вы должны увидеть строки:
   ```
   ✓ Webhook установлен: https://ваш-домен.railway.app/webhook
   ✓ Webhook сервер запущен на порту 8080
   ```
8. Если видите эти строки - все работает! ✅

### 5.2. Проверьте бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по имени, которое вы дали @BotFather)
3. Нажмите **"Start"** или отправьте команду `/start`
4. Бот должен ответить приветственным сообщением
5. Если бот отвечает - все работает! ✅

### 5.3. Если что-то не работает

**Бот не отвечает:**
1. Проверьте логи в Railway (шаг 5.1)
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Убедитесь, что `WEBHOOK_URL` установлен

**Ошибки в логах:**
1. Скопируйте текст ошибки
2. Проверьте, что все переменные добавлены (шаг 3)
3. Убедитесь, что API ключ OpenAI правильный

---

## ШАГ 6: Автоматический деплой (опционально)

Этот шаг нужен, чтобы при каждом изменении кода на GitHub автоматически обновлялся бот на Railway.

### 6.1. Получите Railway Token (обязательно Project Token)

Токен должен быть **именно Project Token**, не из раздела аккаунта — иначе деплой выдаст "Invalid RAILWAY_TOKEN".

1. В Railway откройте **ваш проект** (например, exciting-quietude)
2. Нажмите **настройки проекта** — иконка **шестерёнки** у названия проекта в левой колонке (не вкладка "Settings" у сервиса)
3. В настройках проекта выберите раздел **"Tokens"**
4. Нажмите **"Create Token"** / **"New Token"**
5. Укажите имя (например, `GitHub Actions`) и при создании выберите окружение (например, production)
6. **Скопируйте токен** (показывается один раз; сохраните)
7. **Не используйте** токен с сайта railway.com/account/tokens — он для деплоя не подходит

### 6.2. Получите Service ID

1. В Railway откройте ваш **сервис** (например, worker)
2. Перейдите на вкладку **"Deployments"** или **"Settings"**
3. Посмотрите адрес в браузере — он вида:  
   `https://railway.com/project/.../service/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX?...`
4. **Скопируйте Service ID** — это UUID после `/service/` (до `?` или до конца).  
   Либо в настройках сервиса найдите раздел **"Service ID"** и скопируйте оттуда

### 6.3. Добавьте Secrets в GitHub

1. Откройте ваш репозиторий на GitHub
2. Нажмите на вкладку **"Settings"** (вверху репозитория)
3. В меню слева найдите раздел **"Secrets and variables"**
4. Нажмите на **"Actions"**
5. Нажмите кнопку **"New repository secret"** (зеленая кнопка справа)

**Secret 1:**
- **Name:** `RAILWAY_TOKEN`
- **Secret:** вставьте токен из шага 6.1
- Нажмите **"Add secret"**

**Secret 2:**
- Снова нажмите **"New repository secret"**
- **Name:** `RAILWAY_SERVICE_ID`
- **Secret:** вставьте Service ID из шага 6.2
- Нажмите **"Add secret"**

### 6.4. Проверьте что secrets добавлены

В списке должны быть:
- ✅ `RAILWAY_TOKEN`
- ✅ `RAILWAY_SERVICE_ID`

### 6.5. Проверьте автоматический деплой

1. Внесите любое маленькое изменение в код (например, добавьте пробел в README.md)
2. Закоммитьте и запушьте изменения:
   - В GitHub Desktop: напишите "Test auto-deploy" и нажмите "Commit to main", затем "Push origin"
   - Или через командную строку: `git add .`, `git commit -m "Test"`, `git push`
3. Откройте GitHub → вкладка **"Actions"** (вверху репозитория)
4. Вы увидите запущенный workflow **"Deploy to Railway"**
5. Подождите 1-2 минуты
6. Если статус зеленый ✅ - все работает!

---

## ✅ Чеклист: Все ли готово?

Пройдитесь по списку и убедитесь, что все сделано:

- [ ] Код загружен на GitHub
- [ ] Проект создан на Railway
- [ ] Добавлена переменная `TELEGRAM_BOT_TOKEN`
- [ ] Добавлена переменная `OPENAI_API_KEY`
- [ ] Добавлены переменные `LLM_PROVIDER`, `LLM_MODEL`, `LLM_TEMPERATURE`, `LLM_MAX_TOKENS`
- [ ] Сгенерирован публичный домен
- [ ] Добавлена переменная `WEBHOOK_URL` (без слеша в конце!)
- [ ] В логах видно "✓ Webhook установлен"
- [ ] Бот отвечает в Telegram на команду `/start`
- [ ] (Опционально) Добавлены GitHub Secrets для автоматического деплоя

---

## 🆘 Частые проблемы и решения

### Проблема: Бот не отвечает в Telegram

**Что проверить:**
1. Откройте Railway → ваш сервис → вкладка **"Deployments"** → **"View Logs"**
2. Ищите ошибки (красный текст)
3. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный:
   - Откройте Railway → **"Variables"**
   - Найдите `TELEGRAM_BOT_TOKEN`
   - Убедитесь, что токен начинается с цифр и содержит двоеточие

### Проблема: В логах ошибка "Webhook не установлен"

**Что сделать:**
1. Проверьте, что `WEBHOOK_URL` добавлен:
   - Railway → **"Variables"** → найдите `WEBHOOK_URL`
2. Убедитесь, что URL БЕЗ слеша в конце:
   - ✅ `https://app.railway.app`
   - ❌ `https://app.railway.app/`
3. Убедитесь, что домен сгенерирован:
   - Railway → **"Settings"** → **"Networking"** → должен быть домен

### Проблема: Ошибка "API key invalid"

**Что сделать:**
1. Проверьте `OPENAI_API_KEY`:
   - Railway → **"Variables"** → найдите `OPENAI_API_KEY`
   - Убедитесь, что ключ начинается с `sk-`
2. Проверьте баланс на OpenAI:
   - Зайдите на platform.openai.com
   - Проверьте, есть ли деньги на счету

### Проблема: Автоматический деплой не работает

**Что проверить:**
1. GitHub → **"Settings"** → **"Secrets and variables"** → **"Actions"**
2. Убедитесь, что есть `RAILWAY_TOKEN` и `RAILWAY_SERVICE_ID`
3. GitHub → вкладка **"Actions"** → посмотрите логи workflow
4. Если ошибка - скопируйте текст ошибки и проверьте secrets

---

## 🎉 Готово!

Если все шаги выполнены и бот отвечает в Telegram - поздравляю! 🎊

Ваш бот теперь:
- ✅ Работает на Railway
- ✅ Доступен 24/7
- ✅ Автоматически обновляется при изменении кода (если настроили шаг 6)

**Удачи с вашим ботом! 🚀**

---

## Возможные следующие шаги

После того как деплой и бот работают, можно по желанию сделать:

| Шаг | Описание |
|-----|----------|
| **Проверка переменных при старте** | При запуске бота проверять наличие `TELEGRAM_BOT_TOKEN`, `DATABASE_URL`, `OPENAI_API_KEY` и сразу падать с понятным сообщением в логах, если чего-то нет — так ошибки конфига видны в Railway Logs сразу. |
| **Обновить гайд и секреты** | Убедиться, что в DEPLOYMENT_GUIDE и в GitHub Secrets указаны именно Project Token и корректный RAILWAY_SERVICE_ID (как в шагах 6.1–6.2 выше). |
| **Алерт при падении деплоя** | В workflow GitHub Actions при ошибке деплоя отправлять уведомление (например, в Telegram админу или создать Issue), чтобы не пропустить сбой. |
| **Staging / preview** | Отдельный деплой по PR в отдельное окружение Railway, чтобы проверять изменения до слияния в main. |
| **Резервное копирование БД** | Если Postgres на Railway — настроить периодический дамп (pg_dump) или использовать встроенные бэкапы Railway. |
| **Rate limiting** | Ограничение запросов на пользователя, чтобы один пользователь не перегружал бота и не сжигал лимиты OpenAI. |
| **Аудит зависимостей** | Периодически запускать `pip audit` или `safety` в CI и обновлять уязвимые пакеты. |

Рекомендуется первым делом: **проверка переменных при старте** — небольшая правка в коде, зато при неправильных переменных в Railway ошибка будет видна в логах сразу при старте, а не при первом запросе пользователя.

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Railway (Deployments → View Logs)
2. Проверьте все переменные в Railway (Variables)
3. Проверьте, что бот отвечает в Telegram
4. Если ничего не помогает - скопируйте текст ошибки из логов и попросите помощи
