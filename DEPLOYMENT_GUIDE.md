# 🚀 Простая инструкция по деплою (для начинающих)

Пошаговая инструкция с точными указаниями, куда нажимать.

---

## 📋 Что нужно перед началом

1. ✅ Аккаунт на GitHub (если нет - зарегистрируйтесь на github.com)
2. ✅ Аккаунт на Railway (если нет - зарегистрируйтесь на railway.app)
3. ✅ Telegram Bot Token (получите от @BotFather в Telegram)
4. ✅ API ключ OpenAI (получите на platform.openai.com)

---

## Локальная разработка

Чтобы запустить бота у себя на компьютере (без деплоя):

1. **Клонируйте репозиторий** и перейдите в папку проекта.
2. **Создайте виртуальное окружение** и установите зависимости:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate   # Windows
   # source .venv/bin/activate   # macOS/Linux
   pip install -r requirements.txt
   ```
3. **Настройте переменные окружения.** Скопируйте `.env.example` в `.env` и заполните:
   - `TELEGRAM_BOT_TOKEN` — токен от @BotFather
   - `DATABASE_URL` — например `sqlite:///landing_bot.db` для локальной БД
   - `OPENAI_API_KEY` (или ключ другого провайдера при смене `LLM_PROVIDER`)
   ```bash
   copy .env.example .env   # Windows
   # cp .env.example .env   # macOS/Linux
   ```
   Файл `.env` в репозиторий не коммитьте (он уже в .gitignore).
4. **Запуск:**
   - **Polling (удобно для разработки):** `python main.py` — бот опрашивает Telegram сам.
   - **Webhook (как на Railway):** нужен публичный URL (например, ngrok). См. WEBHOOK_GUIDE.md.

Для тестов: `pip install -r requirements-dev.txt` и `pytest`. Для проверки стиля кода: `ruff check backend/ webhook.py main.py scripts/`.

---

## ШАГ 1: Подготовка кода на GitHub

### 1.1. Откройте GitHub

1. Зайдите на сайт: **github.com**
2. Войдите в свой аккаунт (или зарегистрируйтесь)

### 1.2. Создайте новый репозиторий

1. В правом верхнем углу нажмите на **плюсик (+)** 
2. В выпадающем меню выберите **"New repository"**
3. Заполните:
   - **Repository name**: `telegram-landing-bot` (или любое имя)
   - **Description**: `Telegram bot for landing generation` (опционально)
   - **Public** или **Private** - выберите что хотите
4. **НЕ** ставьте галочки на "Add README", "Add .gitignore" и т.д.
5. Нажмите зеленую кнопку **"Create repository"**

### 1.3. Загрузите код на GitHub

Можно выбрать любой способ — результат один и тот же.

**Вариант А: Через браузер**

В текущем интерфейсе GitHub при **"Upload files"** нет ни кнопок, ни полей, куда можно вписать название папки или путь. Все загруженные файлы попадают только в корень репозитория. Загрузить структуру папок (backend, .github и т.д.) через «Upload files» нельзя.

Через браузер можно только:
- загрузить **файлы из корня** проекта (main.py, webhook.py, requirements.txt, README.md и т.д.) — их перетащите в область загрузки и нажмите **"Commit changes"**;
- либо создавать папки вручную через **"Add file"** → **"Create new file"**: в поле **"Name your file"** вверху страницы введите путь, например `backend/config.py` — тогда создастся папка `backend` и файл в ней. Но тогда содержимое каждого файла нужно вставлять вручную (скопировать из своего файла и вставить в форму). Для десятков файлов это неудобно.

**Вывод:** для этого проекта (с папками backend, .github и т.д.) загрузка только через браузер практически не подходит. Лучше сразу использовать **Вариант Б (GitHub Desktop)** или **Вариант В (командная строка)** — там папки и все файлы загружаются автоматически, без ручного ввода путей.

**Вариант Б: Через GitHub Desktop**

1. Скачайте GitHub Desktop: **desktop.github.com**
2. Установите и откройте
3. Нажмите **"File"** → **"Add Local Repository"**
4. Нажмите **"Choose..."**
5. Выберите папку вашего проекта: `D:\Сайты\creativelandingbotfixed`
6. Нажмите **"Add repository"**
7. Внизу в поле "Summary" напишите: `Initial commit`
8. Нажмите **"Commit to main"** (синяя кнопка внизу)
9. Нажмите **"Publish repository"** (кнопка справа вверху)
10. Выберите ваш репозиторий из списка
11. Нажмите **"Publish repository"**

**Вариант В: Через командную строку**

Откройте командную строку в папке проекта и выполните:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/telegram-landing-bot.git
git push -u origin main
```

(Замените `ВАШ_ЛОГИН` на ваш логин GitHub)

---

## ШАГ 2: Создание проекта на Railway

### 2.1. Войдите в Railway

1. Зайдите на сайт: **railway.app**
2. Нажмите **"Login"** (в правом верхнем углу)
3. Выберите **"Login with GitHub"**
4. Разрешите Railway доступ к вашему GitHub аккаунту

### 2.2. Создайте новый проект

1. На главной странице Railway нажмите большую зеленую кнопку **"New Project"**
2. В появившемся окне выберите **"Deploy from GitHub repo"**
3. Railway покажет список ваших репозиториев
4. Найдите и нажмите на **"telegram-landing-bot"** (или как вы назвали)
5. Railway начнет автоматически создавать проект

### 2.3. Дождитесь создания проекта

1. Вы увидите экран с процессом создания
2. Подождите 1-2 минуты
3. Railway автоматически:
   - Определит что это Python проект
   - Установит зависимости
   - Попытается запустить (но пока не запустится, т.к. нет переменных)

---

## ШАГ 3: Настройка переменных окружения

### 3.1. Откройте настройки сервиса

1. В Railway Dashboard вы увидите ваш проект
2. Нажмите на **сервис** (обычно называется как ваш репозиторий)
3. Вверху вы увидите вкладки: **"Deployments"**, **"Metrics"**, **"Variables"**, **"Settings"**
4. Нажмите на вкладку **"Variables"**

### 3.2. Добавьте Telegram Bot Token

1. Нажмите кнопку **"New Variable"** (справа вверху)
2. В поле **"Name"** введите: `TELEGRAM_BOT_TOKEN`
3. В поле **"Value"** вставьте ваш токен от @BotFather
4. Нажмите **"Add"** (или просто Enter)

**Как получить токен:**
1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3.3. Добавьте OpenAI API Key

1. Снова нажмите **"New Variable"**
2. В поле **"Name"** введите: `OPENAI_API_KEY`
3. В поле **"Value"** вставьте ваш OpenAI API ключ
4. Нажмите **"Add"**

**Как получить OpenAI API ключ:**
1. Зайдите на **platform.openai.com**
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите на ваш профиль (правый верхний угол)
4. Выберите **"API keys"**
5. Нажмите **"Create new secret key"**
6. Скопируйте ключ (начинается с `sk-`)

### 3.4. Добавьте остальные переменные

Нажмите **"New Variable"** для каждой:

**Переменная 1:**
- **Name:** `LLM_PROVIDER`
- **Value:** `openai`
- Нажмите **"Add"**

**Переменная 2:**
- **Name:** `LLM_MODEL`
- **Value:** `gpt-4o`
- Нажмите **"Add"**

**Переменная 3:**
- **Name:** `LLM_TEMPERATURE`
- **Value:** `0.3`
- Нажмите **"Add"**

**Переменная 4:**
- **Name:** `LLM_MAX_TOKENS`
- **Value:** `8000`
- Нажмите **"Add"**

### 3.5. Проверьте что все добавлено

В списке переменных должны быть:
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `OPENAI_API_KEY`
- ✅ `LLM_PROVIDER`
- ✅ `LLM_MODEL`
- ✅ `LLM_TEMPERATURE`
- ✅ `LLM_MAX_TOKENS`

---

## ШАГ 4: Создание публичного домена

### 4.1. Откройте настройки Networking

1. В Railway Dashboard откройте ваш сервис
2. Нажмите на вкладку **"Settings"** (вверху)
3. Прокрутите вниз до раздела **"Networking"**

### 4.2. Сгенерируйте домен

1. В разделе **"Networking"** найдите секцию **"Public Networking"**
2. Нажмите кнопку **"Generate Domain"** (фиолетовая кнопка с иконкой молнии)
3. Railway создаст домен автоматически
4. Домен будет вида: `ваш-проект-production.up.railway.app`
5. **Скопируйте этот URL** (он понадобится на следующем шаге)

**Пример:** `https://creativelandingbot-production-0adf.up.railway.app`

### 4.3. Добавьте WEBHOOK_URL

1. Вернитесь на вкладку **"Variables"**
2. Нажмите **"New Variable"**
3. В поле **"Name"** введите: `WEBHOOK_URL`
4. В поле **"Value"** вставьте скопированный URL (из шага 4.2)
5. **ВАЖНО:** URL должен быть БЕЗ слеша в конце!
   - ✅ Правильно: `https://creativelandingbot-production-0adf.up.railway.app`
   - ❌ Неправильно: `https://creativelandingbot-production-0adf.up.railway.app/`
6. Нажмите **"Add"**

### 4.4. Дождитесь перезапуска

После добавления `WEBHOOK_URL` Railway автоматически перезапустит приложение. Подождите 1-2 минуты.

---

## ШАГ 5: Проверка что все работает

### 5.1. Проверьте логи

1. В Railway Dashboard откройте ваш сервис
2. Нажмите на вкладку **"Deployments"** (вверху)
3. Вы увидите список деплоев
4. Нажмите на **последний деплой** (самый верхний)
5. Нажмите кнопку **"View Logs"** (или просто откройте логи)
6. Прокрутите вниз до конца логов
7. Вы должны увидеть строки:
   ```
   ✓ Webhook установлен: https://ваш-домен.railway.app/webhook
   ✓ Webhook сервер запущен на порту 8080
   ```
8. Если видите эти строки - все работает! ✅

### 5.2. Проверьте бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по имени, которое вы дали @BotFather)
3. Нажмите **"Start"** или отправьте команду `/start`
4. Бот должен ответить приветственным сообщением
5. Если бот отвечает - все работает! ✅

### 5.3. Если что-то не работает

**Бот не отвечает:**
1. Проверьте логи в Railway (шаг 5.1)
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Убедитесь, что `WEBHOOK_URL` установлен

**Ошибки в логах:**
1. Скопируйте текст ошибки
2. Проверьте, что все переменные добавлены (шаг 3)
3. Убедитесь, что API ключ OpenAI правильный

---

## ШАГ 6: Автоматический деплой (опционально)

Этот шаг нужен, чтобы при каждом изменении кода на GitHub автоматически обновлялся бот на Railway.

### 6.1. Получите Railway Token (обязательно Project Token)

Токен должен быть **именно Project Token**, не из раздела аккаунта — иначе деплой выдаст "Invalid RAILWAY_TOKEN".

1. В Railway откройте **ваш проект** (например, exciting-quietude)
2. Нажмите **настройки проекта** — иконка **шестерёнки** у названия проекта в левой колонке (не вкладка "Settings" у сервиса)
3. В настройках проекта выберите раздел **"Tokens"**
4. Нажмите **"Create Token"** / **"New Token"**
5. Укажите имя (например, `GitHub Actions`) и при создании выберите окружение (например, production)
6. **Скопируйте токен** (показывается один раз; сохраните)
7. **Не используйте** токен с сайта railway.com/account/tokens — он для деплоя не подходит

### 6.2. Получите Service ID

1. В Railway откройте ваш **сервис** (например, worker)
2. Перейдите на вкладку **"Deployments"** или **"Settings"**
3. Посмотрите адрес в браузере — он вида:  
   `https://railway.com/project/.../service/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX?...`
4. **Скопируйте Service ID** — это UUID после `/service/` (до `?` или до конца).  
   Либо в настройках сервиса найдите раздел **"Service ID"** и скопируйте оттуда

### 6.3. Добавьте Secrets в GitHub

1. Откройте ваш репозиторий на GitHub
2. Нажмите на вкладку **"Settings"** (вверху репозитория)
3. В меню слева найдите раздел **"Secrets and variables"**
4. Нажмите на **"Actions"**
5. Нажмите кнопку **"New repository secret"** (зеленая кнопка справа)

**Secret 1:**
- **Name:** `RAILWAY_TOKEN`
- **Secret:** вставьте токен из шага 6.1
- Нажмите **"Add secret"**

**Secret 2:**
- Снова нажмите **"New repository secret"**
- **Name:** `RAILWAY_SERVICE_ID`
- **Secret:** вставьте Service ID из шага 6.2
- Нажмите **"Add secret"**

### 6.4. Проверьте что secrets добавлены

В списке должны быть:
- ✅ `RAILWAY_TOKEN`
- ✅ `RAILWAY_SERVICE_ID`

### 6.5. Проверьте автоматический деплой

1. Внесите любое маленькое изменение в код (например, добавьте пробел в README.md)
2. Закоммитьте и запушьте изменения:
   - В GitHub Desktop: напишите "Test auto-deploy" и нажмите "Commit to main", затем "Push origin"
   - Или через командную строку: `git add .`, `git commit -m "Test"`, `git push`
3. Откройте GitHub → вкладка **"Actions"** (вверху репозитория)
4. Вы увидите запущенный workflow **"Deploy to Railway"**
5. Подождите 1-2 минуты
6. Если статус зеленый ✅ - все работает!

---

## ✅ Чеклист: Все ли готово?

Пройдитесь по списку и убедитесь, что все сделано:

- [ ] Код загружен на GitHub
- [ ] Проект создан на Railway
- [ ] Добавлена переменная `TELEGRAM_BOT_TOKEN`
- [ ] Добавлена переменная `OPENAI_API_KEY`
- [ ] Добавлены переменные `LLM_PROVIDER`, `LLM_MODEL`, `LLM_TEMPERATURE`, `LLM_MAX_TOKENS`
- [ ] Сгенерирован публичный домен
- [ ] Добавлена переменная `WEBHOOK_URL` (без слеша в конце!)
- [ ] В логах видно "✓ Webhook установлен"
- [ ] Бот отвечает в Telegram на команду `/start`
- [ ] (Опционально) Добавлены GitHub Secrets для автоматического деплоя

---

## 🆘 Частые проблемы и решения

### Проблема: Бот не отвечает в Telegram

**Что проверить:**
1. Откройте Railway → ваш сервис → вкладка **"Deployments"** → **"View Logs"**
2. Ищите ошибки (красный текст)
3. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный:
   - Откройте Railway → **"Variables"**
   - Найдите `TELEGRAM_BOT_TOKEN`
   - Убедитесь, что токен начинается с цифр и содержит двоеточие

### Проблема: В логах ошибка "Webhook не установлен"

**Что сделать:**
1. Проверьте, что `WEBHOOK_URL` добавлен:
   - Railway → **"Variables"** → найдите `WEBHOOK_URL`
2. Убедитесь, что URL БЕЗ слеша в конце:
   - ✅ `https://app.railway.app`
   - ❌ `https://app.railway.app/`
3. Убедитесь, что домен сгенерирован:
   - Railway → **"Settings"** → **"Networking"** → должен быть домен

### Проблема: Ошибка "API key invalid"

**Что сделать:**
1. Проверьте `OPENAI_API_KEY`:
   - Railway → **"Variables"** → найдите `OPENAI_API_KEY`
   - Убедитесь, что ключ начинается с `sk-`
2. Проверьте баланс на OpenAI:
   - Зайдите на platform.openai.com
   - Проверьте, есть ли деньги на счету

### Проблема: Автоматический деплой не работает

**Что проверить:**
1. GitHub → **"Settings"** → **"Secrets and variables"** → **"Actions"**
2. Убедитесь, что есть `RAILWAY_TOKEN` и `RAILWAY_SERVICE_ID`
3. GitHub → вкладка **"Actions"** → посмотрите логи workflow
4. Если ошибка - скопируйте текст ошибки и проверьте secrets

---

## 🎉 Готово!

Если все шаги выполнены и бот отвечает в Telegram - поздравляю! 🎊

Ваш бот теперь:
- ✅ Работает на Railway
- ✅ Доступен 24/7
- ✅ Автоматически обновляется при изменении кода (если настроили шаг 6)

**Удачи с вашим ботом! 🚀**

---

## Возможные следующие шаги

После того как деплой и бот работают, можно по желанию сделать:

| Шаг | Статус | Описание |
|-----|--------|----------|
| **Проверка переменных при старте** | ✅ Сделано | `Config.validate()` в `main.py` и `webhook.py` — при отсутствии `TELEGRAM_BOT_TOKEN`, `DATABASE_URL` или ключа LLM бот падает при старте с понятным сообщением в логах. |
| **Алерт при падении деплоя** | ✅ Сделано | В `.github/workflows/deploy.yml` job `notify-deploy-failure`: при ошибке деплоя в Telegram отправляется уведомление (нужны секреты `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ADMIN_CHAT_ID`). |
| **Аудит зависимостей** | ✅ Сделано | В CI job **Dependency audit** запускается `pip-audit` для `requirements.txt`. См. раздел «Аудит зависимостей» ниже. |
| **Rate limiting** | ✅ Сделано | В `backend/utils/rate_limiter.py`: лимит генераций на пользователя за период (по умолчанию `MAX_REQUESTS_PER_HOUR=10` в `.env`), проверка по БД перед генерацией. |
| **Lint в CI** | ✅ Сделано | В CI job **Lint** запускается `ruff check` для `backend/`, `webhook.py`, `main.py`, `scripts/`. Настройки в `pyproject.toml`. |
| **Шаблон .env и локальный запуск** | ✅ Сделано | Файл `.env.example` — список переменных без значений. В гайде раздел «Локальная разработка»: venv, установка, копирование .env, запуск `main.py` или webhook. |
| **Обновить гайд и секреты** | По необходимости | Убедиться, что в DEPLOYMENT_GUIDE и в GitHub Secrets указаны именно Project Token и корректный RAILWAY_SERVICE_ID (как в шагах 6.1–6.2 выше). |
| **Staging / preview** | Опционально | Отдельный деплой по PR в отдельное окружение Railway. См. раздел «Staging / Preview» ниже. |
| **Резервное копирование БД** | Опционально | Если Postgres на Railway — периодический дамп или встроенные бэкапы. См. раздел «Резервное копирование БД» ниже. |

---

### Staging / Preview

Workflow **Deploy to Staging (Preview)** при каждом PR в `main`/`master` деплоит ветку PR в отдельный сервис Railway, чтобы проверять изменения до слияния.

#### Зачем это нужно

- Увидеть, как бот ведёт себя на реальном Railway с кодом из PR.
- Не трогать production: падения и эксперименты только в staging.
- CI уже гоняет тесты на PR; staging даёт «живой» бот по ссылке.

#### Пошаговая настройка

**Шаг 1. Создать staging-сервис в Railway**

1. Откройте [Railway Dashboard](https://railway.app/dashboard), выберите тот же проект, где уже развёрнут бот (или создайте новый проект для staging).
2. В проекте нажмите **+ New** → **Empty Service** (или **Deploy from GitHub repo** и укажите тот же репозиторий). Назовите сервис, например, `landing-bot-staging`.
3. Для этого сервиса настройте деплой так же, как для прода: источник — ваш GitHub-репозиторий, ветка по умолчанию может быть `main` (деплой из GitHub по PR будет переопределяться нашим workflow).
4. Откройте **Variables** у staging-сервиса. Скопируйте сюда переменные из production-сервиса (TELEGRAM_BOT_TOKEN, DATABASE_URL, OPENAI_API_KEY и т.д.). Варианты:
   - **Тестовый бот:** создайте отдельного бота через [@BotFather](https://t.me/BotFather), в Variables у staging подставьте его токен и при необходимости отдельную тестовую БД.
   - **Та же БД (осторожно):** можно временно указать тот же DATABASE_URL, чтобы не поднимать второй Postgres; тогда тестовые данные будут в общей БД.
5. Сохраните Variables.

**Шаг 2. Узнать Service ID staging-сервиса**

1. В Railway откройте **staging-сервис** (клик по сервису в проекте).
2. В адресной строке браузера будет URL вида:  
   `https://railway.app/project/<PROJECT_ID>/service/<SERVICE_ID>`  
   либо в интерфейсе: **Settings** → внизу блок **Service** — там указан **Service ID** (UUID, например `a1b2c3d4-e5f6-7890-abcd-ef1234567890`).
3. Скопируйте этот **Service ID** — он понадобится как значение секрета `RAILWAY_STAGING_SERVICE_ID`.

**Шаг 3. Добавить секреты в GitHub**

1. Откройте репозиторий на GitHub → **Settings** → **Secrets and variables** → **Actions**.
2. Нажмите **New repository secret**.
3. Имя: `RAILWAY_STAGING_SERVICE_ID`, значение: вставьте скопированный Service ID staging-сервиса. Сохраните.
4. (Опционально) Если staging в **другом проекте** Railway: создайте ещё два секрета — `RAILWAY_STAGING_PROJECT_ID` и `RAILWAY_STAGING_ENVIRONMENT_ID`. Их можно взять из URL проекта и окружения в Railway (Settings проекта / окружения).
5. Для деплоя staging используется тот же **RAILWAY_TOKEN**, что и для прода (уже должен быть в секретах). Если хотите изолировать права — создайте в Railway отдельный Project Token и сохраните его как секрет с тем же именем `RAILWAY_TOKEN` только для этого репозитория (тогда он будет использоваться и для прода, и для staging, если один workflow; либо настройте отдельный токен только для staging через переменные workflow — сложнее).

**Шаг 4. Проверка**

1. Создайте в репозитории ветку, внесите любое изменение, откройте **Pull Request** в `main` (или `master`).
2. Перейдите на вкладку **Actions**. Должен появиться запуск workflow **Deploy to Staging (Preview)**.
3. Дождитесь окончания. Если секрет `RAILWAY_STAGING_SERVICE_ID` задан, шаг «Deploy to Railway Staging» задеплоит код из ветки PR в staging-сервис. В Railway у staging-сервиса во вкладке **Deployments** появится новый деплой.
4. Если секрет не задан, выполнится job **skip-info** с сообщением о том, что staging отключён — это нормально, основной CI (тесты) при этом работает как обычно.

**Важно:** Railway по умолчанию может деплоить сервис из ветки `main` при пуше. Наш workflow при PR пушит образ из ветки PR. Чтобы не было путаницы, убедитесь, что в настройках деплоя staging-сервиса в Railway указан тот же репозиторий; источник кода для «preview» в данном случае управляется GitHub Actions (через `railway up` из ветки PR).

---

### Резервное копирование БД

Рекомендуется настроить бэкапы, чтобы при сбое или случайном удалении БД на Railway можно было восстановить данные.

#### Автоматический бэкап (GitHub Actions)

Workflow **Backup Database** (файл `.github/workflows/backup-db.yml`):

- **Когда запускается:** каждый день в **02:00 UTC** и вручную по кнопке **Run workflow**.
- **Что делает:** подключается к PostgreSQL по строке `DATABASE_URL`, выполняет `pg_dump` в формате custom (сжатый `.dump`), загружает файл как артефакт GitHub Actions.
- **Срок хранения артефактов:** 14 дней (потом GitHub удаляет их автоматически).

##### Пошаговая настройка автоматического бэкапа

**Шаг 1. Узнать DATABASE_URL в Railway**

1. Откройте [Railway Dashboard](https://railway.app/dashboard) → ваш проект.
2. Выберите сервис **PostgreSQL** (не бот, а именно БД).
3. Откройте вкладку **Variables** (или **Connect**). Там будет переменная **DATABASE_URL** или **DATABASE_PRIVATE_URL** (предпочтительно приватный URL, он не проходит через публичный прокси). Значение выглядит так:  
   `postgresql://postgres:пароль@hostname.railway.app:5432/railway`
4. Нажмите **Reveal** (если скрыто) и скопируйте значение целиком. Не публикуйте его и не коммитьте в репозиторий.

**Шаг 2. Добавить секрет в GitHub**

1. Репозиторий на GitHub → **Settings** → **Secrets and variables** → **Actions**.
2. **New repository secret**.
3. Имя: **DATABASE_URL**. Значение: вставьте скопированную строку подключения. Сохраните.

**Шаг 3. Проверка (ручной запуск)**

1. Перейдите на вкладку **Actions** в репозитории.
2. В левом списке выберите workflow **Backup Database**.
3. Справа нажмите **Run workflow** → **Run workflow**.
4. Дождитесь завершения (зелёная галочка). Откройте последний run → внизу страницы блок **Artifacts**. Должен появиться артефакт вида `db-backup-YYYYMMDD_HHMMSS`. Нажмите на него, чтобы скачать архив с файлом `.dump`.

Если секрет **DATABASE_URL** не задан, workflow всё равно завершится успешно, но в логе будет сообщение, что бэкап пропущен из‑за отсутствия DATABASE_URL.

**Восстановление из дампа (кратко):**  
На машине с установленным `pg_restore` и доступом к БД (например, новая БД на Railway):  
`pg_restore -d "$DATABASE_URL" --no-owner --no-acl db_backup_YYYYMMDD_HHMMSS.dump`  
(подставьте свой URL и имя файла). Подробности — в [документации PostgreSQL](https://www.postgresql.org/docs/current/app-pgrestore.html).

#### Ручной дамп (локально)

Если нужно сделать разовый дамп со своей машины (например, перед крупным обновлением):

1. Установите клиент PostgreSQL:  
   - Windows: установка [PostgreSQL](https://www.postgresql.org/download/windows/) или [pg_dump в PATH](https://stackoverflow.com/questions/7046440/how-to-use-pg-dump);  
   - macOS: `brew install libpq` и при необходимости добавьте в PATH;  
   - Linux: `sudo apt install postgresql-client` (или аналог).
2. В терминале перейдите в корень репозитория. Выполните:
   ```bash
   export DATABASE_URL='postgresql://...'   # вставьте свою строку из Railway
   chmod +x scripts/backup_db.sh
   ./scripts/backup_db.sh
   ```
   В текущей папке появится файл `db_backup_YYYYMMDD_HHMMSS.dump`. Храните его в безопасном месте и не коммитьте в Git.

#### Встроенные бэкапы Railway

На платных планах Railway у сервиса PostgreSQL в панели проекта может быть раздел **Backups** (или аналогичный). Там можно включить автоматические снепшоты по расписанию. Это дополняет дампы через GitHub Actions: два независимых способа бэкапа надёжнее.

---

### Аудит зависимостей (pip-audit)

В пайплайне CI добавлена задача **Dependency audit**: при каждом push и PR зависимости из `requirements.txt` проверяются на известные уязвимости (через [pip-audit](https://pypi.org/project/pip-audit/), база PyPI/OSV). Если найдены уязвимости, job **audit** падает, и в логе Actions будет список пакетов и CVE.

**Что делать, если audit упал:**

1. Откройте вкладку **Actions** → выберите упавший workflow → job **Dependency audit** и посмотрите вывод шага «Audit production dependencies»: там будут указаны пакет, версия и идентификатор уязвимости (например, `PYSEC-2024-123` или `GHSA-xxxx`).
2. Обновите уязвимый пакет до безопасной версии (в выводе pip-audit часто подсказывает «fix available: X.Y.Z»):
   ```bash
   pip install --upgrade "имя-пакета>=безопасная-версия"
   ```
3. Зафиксируйте новую версию в `requirements.txt` (рекомендуется точное закрепление `пакет==X.Y.Z`).
4. Запустите локально проверку перед пушем:
   ```bash
   pip install pip-audit
   pip-audit -r requirements.txt
   ```
5. Закоммитьте обновлённый `requirements.txt` и запушьте — CI снова пройдёт после устранения уязвимостей.

При необходимости временно игнорировать конкретную уязвимость (например, ложное срабатывание) в CI можно использовать опцию `ignore-vulns` у [pypa/gh-action-pip-audit](https://github.com/pypa/gh-action-pip-audit); делать это стоит только при обоснованной причине.

---

## Текущее состояние и что сделать (генерация лендинга)

**Где мы сейчас:**

- **Сделано:** при выборе «Один лендинг» бот больше не восстанавливает старый диалог — создаётся новый агент, показывается приветствие (главное фото → описание → остальные фото → мини-опрос → «Да, генерировать»). Правка в коде: `force_new=True` при вызове `start_ai_agent` из выбора режима.
- **Проблема:** после нажатия «Да, генерировать» архив с лендингом пользователю не приходит. По логам, которые были раньше, видно только старт и выбор режима; логов в момент нажатия «Да, генерировать» не было — поэтому точная причина (падение генерации, отсутствие zip или ошибка отправки) пока не установлена.

**Что сделать по шагам:**

1. **Задеплоить последние изменения** (если ещё не делали)  
   Запушьте в ветку **main** того репозитория, с которого идёт деплой на Railway. Так на прод попадёт правка с новым диалогом (`force_new`).

2. **Снять логи в момент генерации**  
   В боте: /ai → выбрать «Один лендинг» → пройти шаги (фото, описание, товар и т.д.) → нажать **«Да, генерировать»**.  
   Сразу откройте **Railway → ваш сервис → Deployments → View Logs** и посмотрите строки за эти секунды. Ищите:
   - `Starting generation for user ...` — запрос на генерацию дошёл.
   - `Generation result for user ... success=True` или `success=False`, `error=...`
   - При `success=True`: `Sending zip file...` и `Zip file sent successfully` (архив должен прийти) или `Error sending zip file` / `Zip file not found`.

3. **Определить причину по логам**
   - **`success=False` и `error=...`** — падает генерация (LLM, валидация кода и т.д.). В чате пользователь должен увидеть «Ошибка генерации». Проверьте в Railway переменные: `OPENAI_API_KEY` (или другой провайдер по `LLM_PROVIDER`), квоты API.
   - **`Zip file not found`** — генерация прошла, но путь к zip неверный или файл не создаётся. Проверьте `FILES_DIR`; на Railway без тома файлы живут только до перезапуска контейнера.
   - **`Error sending zip file`** — архив собран, но не удалось отправить в Telegram (размер до 50 MB, таймаут, сеть). В логах будет текст исключения.

4. **Убедиться, что до кнопки доходите**  
   После выбора «Один лендинг» должно прийти приветствие с просьбой прислать главное фото и описание. Если приходит «Диалог восстановлен» — правка с `force_new` ещё не на проде; запушьте `main` и дождитесь деплоя.

После шагов 2–3 будет ясно, на каком этапе обрыв (генерация → создание zip → отправка), и можно точечно править код или настройки.

---

## Фото в лендинге (zip-архив)

**Да, фотки, которые клиент скидывает в чат боту, уже добавляются в генерируемый лендинг и в zip-архив.** Это реализовано так:

1. **Сохранение файлов:** при отправке фото/видео в AI-режиме бот скачивает их на сервер в папку `FILES_DIR` (по умолчанию `generated_landings`) с именами вида `temp_{user_id}_{file_unique_id}.jpg` и передаёт путь агенту.
2. **Агент:** хранит пути в `collected_data['files']` (с полем `block`: hero, gallery, description, review и т.д.).
3. **Перед генерацией:** метод `convert_to_user_data()` в агенте формирует из этих путей поля `hero_media`, `middle_gallery`, `description_photos`, `reviews` и т.п.
4. **Генератор:** после сохранения HTML/CSS/JS в папку проекта вызывает `_copy_user_media(project_dir, user_data)` — копирует все файлы по путям из `user_data` в подпапку `img/` проекта (hero.jpg, gallery_1.jpg, description_1.jpg, review_1.jpg и т.д.).
5. **Zip:** создаётся из всей папки проекта (включая `img/`), поэтому фото попадают в архив.

**Какие файлы это регулируют:**

| Что | Файл |
|-----|------|
| Скачивание фото из Telegram и путь во временный файл | `backend/bot/telegram_bot.py` (поиск по `download_to_drive`, `temp_`) |
| Хранение путей в агенте и разметка hero/gallery/description | `backend/bot/ai_agent.py` (`_process_files`, `convert_to_user_data`) |
| Копирование медиа в папку проекта и создание zip | `backend/generator/code_generator.py` (`_copy_user_media`, `_save_files`, `_create_zip`) |
| Папка для файлов на сервере | `backend/config.py` — `FILES_DIR` (переменная окружения `FILES_DIR`, по умолчанию `generated_landings`) |

**Важно на Railway:** если `FILES_DIR` указывает на обычную папку внутри контейнера (без volume), при перезапуске контейнера временные файлы пропадают. Генерация должна успеть завершиться в той же сессии, где пользователь отправил фото. Для долгой работы подойдёт настройка тома (Volume) в Railway для `FILES_DIR` или использование внешнего хранилища.

---

## Почему бот не отправляет лендинг (zip)

Типичные причины и что проверить:

1. **Генерация падает с ошибкой**  
   В логах Railway в момент нажатия «Да, генерировать» ищите строки:
   - `Starting generation for user ...` → `Generation result ... success=False` или `error=...`
   - `Ошибка при генерации`, `Error generating`, исключения из `code_generator.generate` или LLM.  
   Если видите `success=False` или traceback — причина в генерации (LLM, валидация кода, нехватка данных и т.д.).

2. **Zip не найден**  
   В логах: `Zip file not found for user ...` или `файл не найден`. Значит либо генерация не вернула путь к zip, либо файл создаётся в другом месте (проверьте `FILES_DIR` и что на сервере нет ошибок записи).

3. **Ошибка при отправке в Telegram**  
   Строка вида `Error sending zip file to user ...` — тогда лендинг собран, но `send_document` упал (размер, таймаут, сетевая ошибка).

4. **Нет фото / не прошла валидация**  
   Если бот пишет «нужна хотя бы одна фотография» или «Обнаружены ошибки в данных» — до вызова генерации дело не доходит; нужно дособрать данные.

Рекомендация: в Railway откройте **Deployments → View Logs**, нажмите в боте «Да, генерировать» и сразу смотрите логи за эти секунды — по ним будет видно, на каком шаге обрыв (генерация, создание zip, отправка).

---

## Деплой и репозитории (CI vs Deploy)

В **этом** проекте один репозиторий. В нём два типа workflow:

| Workflow | Файл | Когда запускается | Что делает |
|----------|------|-------------------|------------|
| **CI** | `.github/workflows/ci.yml` | Push и Pull Request в `main`, `master`, `develop` | Запускает **тесты** (pytest), линтер (ruff), проверку зависимостей (pip-audit). **Ничего не деплоит.** |
| **Deploy** | `.github/workflows/deploy.yml` | Push в ветки `main` или `master` | Деплоит код на **Railway** (продакшен). |

Итого:

- **Чтобы задеплоить новый код на продакшен:** пушите изменения в ветку **main** (или **master**) **того репозитория, который подключён к Railway** (обычно это ваш основной репозиторий с ботом). После push автоматически запустится **Deploy to Railway** и зальётся актуальный код.
- **Второй репозиторий под тесты:** если у вас отдельный репо «только для тестов» (например, форк или копия), то при push туда обычно запускается только **CI** (тесты). Такой репозиторий **не деплоит** продакшен — деплой идёт из того репо, куда вы пушите `main` и где настроены секреты Railway (`RAILWAY_TOKEN`, `RAILWAY_SERVICE_ID` и т.д.). Файлы тестов в zip на Railway не попадают: при деплое используется код репозитория (исходники и конфиги), тесты нужны только для прогонки в GitHub Actions.

**Кратко:** после прогонки тестов (в любом репо) для выката изменений на прод нужно пушить в **main первого (основного) репозитория** — оттуда идёт деплой на Railway.

---

## Чеклист: проверка генерации лендинга (Вариант А)

После изменений в промптах и структуре лендинга полезно проверить генерацию по шагам.

### Автоматические тесты

- Запуск тестов промпта и генератора (локально или в CI):
  ```bash
  pytest tests/test_prompt_builder.py tests/test_code_generator.py -v
  ```
- Проверяется: промпт содержит блоки 1–8, нет требований «500+ строк», есть формулировки про complete/production-ready.

### Ручная проверка через бота

1. **Запуск бота** (локально или на Railway).
2. **Режим «один товар»** → отправить главное фото и описание товара → заполнить мини-опрос (скидка, цены, подвал, форма, распределение фото, при необходимости видео).
3. **Нажать «Да, генерировать»** и дождаться архива с лендингом.

### Что проверить в сгенерированном лендинге

| № | Проверка | Где смотреть |
|---|----------|--------------|
| 1 | **Порядок блоков** | Открыть index.html: Hero (фото + скидка + форма) → при наличии видео блок сразу под hero → Описание → Средняя форма → при наличии фото галерея (карусель) → Отзывы → Дубль hero (фото + форма) → Подвал (ИП/ООО, УНП, адрес, email, время работы). |
| 2 | **Видео** | Если загружали видео: блок с видео сразу под hero; воспроизведение при попадании в зону видимости (проверить при скролле). |
| 3 | **Формы** | Одна и та же форма (имя, телефон, при наличии размер/цвет/кол-во) в hero, в середине и внизу; action="send.php". |
| 4 | **Галерея** | Если загружали ≥2 фото для галереи: карусель, полноформатные 3:4, кнопки вперёд/назад, индикаторы. |
| 5 | **Отзывы** | Блок отзывов; при наличии фото отзывов — соотношение 3:4. |
| 6 | **Качество кода** | CSS: все секции оформлены, есть переменные (:root), цвета и шрифты; нет пустых блоков. JS: таймер (если включён), маска телефона +375, карусели, валидация форм. |
| 7 | **Данные** | Название товара, цены, описание, данные подвала (УНП, адрес, телефон, email) — без заглушек, как введены в боте. |

При расхождениях: проверить логи (Railway или консоль при локальном запуске), при необходимости скорректировать промпты или логику сбора данных.

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Railway (Deployments → View Logs)
2. Проверьте все переменные в Railway (Variables)
3. Проверьте, что бот отвечает в Telegram
4. Если ничего не помогает - скопируйте текст ошибки из логов и попросите помощи
