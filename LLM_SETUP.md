# Настройка LLM провайдеров для генерации лендингов

## 📋 Обзор

Проект поддерживает три провайдера для генерации кода лендингов:
- **OpenAI** (GPT-4o, GPT-4-turbo) - рекомендуется
- **Anthropic Claude** (claude-3-5-sonnet-20241022) - альтернатива
- **Google Gemini** (gemini-1.5-pro) - альтернатива

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Это установит:
- `openai==1.6.0` - для OpenAI
- `anthropic==0.18.1` - для Claude (опционально)
- `google-generativeai==0.3.2` - для Gemini (опционально)

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта и добавьте нужные ключи:

#### Для OpenAI (рекомендуется):
```env
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-api-key-here
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8000
```

#### Для Anthropic Claude:
```env
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8000
```

#### Для Google Gemini:
```env
LLM_PROVIDER=google
GOOGLE_API_KEY=your-google-api-key-here
LLM_MODEL=gemini-1.5-pro
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8000
```

## 🔑 Получение API ключей

### OpenAI
1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API keys"
4. Создайте новый ключ
5. Скопируйте ключ (начинается с `sk-`)

**Рекомендуемые модели:**
- `gpt-4o` - самая новая и мощная модель (рекомендуется)
- `gpt-4-turbo` - быстрая и качественная
- `gpt-4` - стабильная, но медленнее

### Anthropic Claude
1. Перейдите на https://console.anthropic.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Создайте новый ключ
5. Скопируйте ключ (начинается с `sk-ant-`)

**Рекомендуемые модели:**
- `claude-3-5-sonnet-20241022` - самая новая модель (рекомендуется)
- `claude-3-opus-20240229` - мощная, но дороже
- `claude-3-sonnet-20240229` - баланс качества и скорости

### Google Gemini
1. Перейдите на https://makersuite.google.com/app/apikey
2. Войдите через Google аккаунт
3. Создайте новый API ключ
4. Скопируйте ключ

**Рекомендуемые модели:**
- `gemini-1.5-pro` - самая мощная модель (рекомендуется)
- `gemini-1.5-flash` - быстрая альтернатива

## ⚙️ Настройки параметров

### LLM_TEMPERATURE
- **0.0-0.3**: Более точные и детерминированные результаты (рекомендуется для кода)
- **0.4-0.7**: Баланс между креативностью и точностью
- **0.8-1.0**: Более креативные, но менее предсказуемые результаты

**Рекомендуемое значение: 0.3** - для генерации кода нужна точность.

### LLM_MAX_TOKENS
- **4000-6000**: Базовый уровень (может не хватить для полного кода)
- **8000-10000**: Рекомендуется для полных лендингов
- **16000+**: Для очень больших проектов

**Рекомендуемое значение: 8000** - достаточно для полного HTML/CSS/JS.

## 🔄 Переключение между провайдерами

Просто измените `LLM_PROVIDER` в `.env`:
```env
LLM_PROVIDER=openai      # или anthropic, или google
```

И убедитесь, что соответствующий API ключ установлен.

## 📊 Сравнение провайдеров

| Провайдер | Плюсы | Минусы | Рекомендация |
|-----------|-------|--------|--------------|
| **OpenAI GPT-4o** | Отличное качество, быстрая генерация, хорошая поддержка JSON | Платный | ⭐⭐⭐⭐⭐ Рекомендуется |
| **Anthropic Claude** | Очень качественный код, отличное понимание контекста | Дороже, медленнее | ⭐⭐⭐⭐ Альтернатива |
| **Google Gemini** | Бесплатный тариф, хорошее качество | Может быть менее стабильным | ⭐⭐⭐ Бюджетный вариант |

## 🐛 Решение проблем

### Ошибка: "API ключ не установлен"
- Проверьте, что ключ правильно указан в `.env`
- Убедитесь, что файл `.env` находится в корне проекта
- Проверьте, что нет лишних пробелов в ключе

### Ошибка: "Модель не найдена"
- Проверьте правильность названия модели
- Убедитесь, что модель доступна в вашем тарифе
- Для OpenAI: `gpt-4o`, `gpt-4-turbo`, `gpt-4`
- Для Claude: `claude-3-5-sonnet-20241022`
- Для Gemini: `gemini-1.5-pro`

### Генерируется неполный код
- Увеличьте `LLM_MAX_TOKENS` до 8000-10000
- Снизьте `LLM_TEMPERATURE` до 0.2-0.3
- Попробуйте другую модель (например, `gpt-4o` вместо `gpt-4-turbo`)

### Плохое качество кода
- Используйте более новую модель (`gpt-4o` или `claude-3-5-sonnet-20241022`)
- Снизьте температуру до 0.2-0.3
- Проверьте, что промпт содержит все необходимые данные

## 💡 Советы по использованию

1. **Начните с OpenAI GPT-4o** - это самая надежная опция
2. **Используйте температуру 0.3** - для кода нужна точность
3. **Установите max_tokens 8000** - для полных лендингов
4. **Проверяйте сгенерированный код** - всегда валидируйте результат
5. **Используйте fallback-шаблоны** - если LLM вернул неполный код

## 📝 Пример конфигурации .env

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# LLM Configuration (OpenAI)
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-key-here
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8000

# Database
DATABASE_URL=sqlite:///landing_bot.db

# File Storage
FILES_DIR=generated_landings
```

## 🔐 Безопасность

- **НЕ коммитьте файл `.env` в Git!**
- Добавьте `.env` в `.gitignore`
- Храните API ключи в секрете
- Регулярно ротируйте ключи
- Используйте ограничения по IP в настройках API (если доступно)

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи в `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте баланс на счету API провайдера
4. Попробуйте другой провайдер или модель

