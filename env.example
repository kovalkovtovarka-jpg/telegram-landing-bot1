# ============================================
# Telegram Landing Bot - Переменные окружения
# ============================================
# Скопируйте этот файл в .env и заполните значения
# cp env.example .env

# ============================================
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
# ============================================

# Telegram Bot Token (получите от @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# LLM Provider (openai, anthropic, google)
LLM_PROVIDER=openai

# LLM API Key (в зависимости от провайдера)
# Для OpenAI:
OPENAI_API_KEY=your_openai_api_key_here

# Для Anthropic Claude (если LLM_PROVIDER=anthropic):
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Для Google Gemini (если LLM_PROVIDER=google):
# GOOGLE_API_KEY=your_google_api_key_here

# ============================================
# БАЗА ДАННЫХ
# ============================================

# SQLite (для локальной разработки):
# DATABASE_URL=sqlite:///landing_bot.db

# PostgreSQL (для продакшн - ОБЯЗАТЕЛЬНО):
DATABASE_URL=postgresql://user:password@host:5432/database

# Примеры облачных PostgreSQL:
# ElephantSQL: postgresql://user:pass@host.elephantsql.com:5432/dbname
# Supabase: postgresql://postgres:password@host.supabase.co:5432/postgres
# Neon: postgresql://user:pass@host.neon.tech/dbname

# ============================================
# НАСТРОЙКИ LLM (опционально)
# ============================================

# Модель LLM
LLM_MODEL=gpt-4o

# Температура генерации (0.0-2.0, по умолчанию 0.3)
LLM_TEMPERATURE=0.3

# Максимальное количество токенов (по умолчанию 8000)
LLM_MAX_TOKENS=8000

# Таймаут для LLM запросов в секундах (по умолчанию 120)
LLM_TIMEOUT=120

# Максимальное количество попыток при ошибке (по умолчанию 3)
LLM_MAX_RETRIES=3

# Начальная задержка между попытками в секундах (по умолчанию 2.0)
LLM_RETRY_DELAY=2.0

# ============================================
# ХРАНИЛИЩЕ ФАЙЛОВ (опционально)
# ============================================

# Директория для сохранения сгенерированных лендингов
FILES_DIR=generated_landings

# Максимальный размер файла в байтах (по умолчанию 10MB)
MAX_FILE_SIZE=10485760

# ============================================
# RATE LIMITING (опционально)
# ============================================

# Максимальное количество запросов в час на пользователя
MAX_REQUESTS_PER_HOUR=10

# ============================================
# АДМИНИСТРАТОРЫ (опционально)
# ============================================

# ID администраторов через запятую (для специальных функций)
# Установлен ваш ID как администратор:
BOT_ADMIN_IDS=979250893
# Пример с несколькими администраторами:
# BOT_ADMIN_IDS=979250893,123456789,987654321

# Уведомлять админов в Telegram при каждом старте бота (1/true = да)
# NOTIFY_ADMINS_ON_STARTUP=1

# ============================================
# WEBHOOK (ОБЯЗАТЕЛЬНО для продакшн с Procfile)
# ============================================

# URL для webhook (ОБЯЗАТЕЛЬНО для webhook режима)
# Требуется HTTPS домен
# Для Railway: https://your-app-name.railway.app
# Для Render: https://your-app-name.onrender.com
# ВАЖНО: Замените на реальный URL вашего приложения после деплоя!
WEBHOOK_URL=https://your-app-name.railway.app

# Порт для webhook сервера
# На Railway/Render обычно используется переменная PORT автоматически
# НЕ указывайте вручную, если используете Railway/Render
# Если нужно указать вручную (для других платформ):
# WEBHOOK_PORT=8443

# ============================================
# ЛОГИРОВАНИЕ (опционально)
# ============================================

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Путь к файлу логов
# LOG_FILE=bot.log

# Максимальный размер файла логов в байтах (по умолчанию 10MB)
# LOG_MAX_BYTES=10485760

# Количество резервных файлов логов (по умолчанию 5)
# LOG_BACKUP_COUNT=5

# Максимальная длина промпта в символах (по умолчанию 15000)
# Если промпт превышает этот размер, он будет автоматически сжат
# MAX_PROMPT_LENGTH=15000

# ============================================
# ПРИМЕЧАНИЯ
# ============================================
# 1. Никогда не коммитьте .env файл в Git!
# 2. Для продакшн используйте PostgreSQL вместо SQLite
# 3. Webhook рекомендуется для продакшн (более эффективен чем polling)
# 4. Убедитесь что все обязательные переменные заполнены перед запуском

