# 🔗 Как получить WEBHOOK_URL

## 📌 Что такое WEBHOOK_URL?

`WEBHOOK_URL` — это **публичный URL вашего приложения** после деплоя на платформу (Railway, Render и т.д.). Это адрес, по которому Telegram будет отправлять обновления вашему боту.

**Формат:** `https://your-app-name.railway.app` (без `/webhook` в конце!)

---

## 🚀 Пошаговая инструкция

### Вариант 1: Railway (Рекомендуется)

#### Шаг 1: Создайте проект на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий с ботом

#### Шаг 2: Дождитесь деплоя

Railway автоматически:
- Определит `Procfile`
- Установит зависимости
- Запустит приложение

#### Шаг 3: Сгенерируйте публичный домен

1. В Railway откройте ваш проект
2. Перейдите на вкладку **"Settings"**
3. Найдите раздел **"Networking"**
4. В секции **"Public Networking"** нажмите кнопку **"Generate Domain"** (фиолетовая кнопка с иконкой молнии)
5. Railway автоматически создаст публичный URL вида: `https://your-app-name.railway.app`
6. Скопируйте этот URL — это и есть ваш `WEBHOOK_URL`!

**Альтернативный способ:**

1. Перейдите на вкладку **"Deployments"**
2. Откройте последний деплой
3. В логах найдите строку типа:
   ```
   ✓ Webhook сервер запущен на порту XXXX
   ```
4. URL будет показан в разделе **"Networking"** → **"Public Networking"**

#### Шаг 4: Скопируйте URL

Скопируйте URL (например: `https://creativelandingbot-production.up.railway.app`)

#### Шаг 5: Установите переменную окружения

1. В Railway откройте проект
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"New Variable"**
4. Введите:
   - **Name:** `WEBHOOK_URL`
   - **Value:** `https://your-app-name.railway.app` (ваш реальный URL)
5. Нажмите **"Add"**

#### Шаг 6: Перезапустите приложение

После установки `WEBHOOK_URL` Railway автоматически перезапустит приложение.

---

### Вариант 2: Render

#### Шаг 1: Создайте Web Service

1. Зайдите на [render.com](https://render.com)
2. Войдите через GitHub
3. Нажмите **"New +"** → **"Web Service"**
4. Подключите ваш репозиторий

#### Шаг 2: Настройте сервис

- **Name:** `creativelandingbot` (или любое имя)
- **Environment:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python webhook.py`

#### Шаг 3: Дождитесь деплоя

Render автоматически задеплоит приложение.

#### Шаг 4: Найдите URL

1. После деплоя Render автоматически создаст URL
2. URL будет показан вверху страницы сервиса
3. Формат: `https://your-app-name.onrender.com`

#### Шаг 5: Установите переменную окружения

1. В Render откройте ваш сервис
2. Перейдите на вкладку **"Environment"**
3. Нажмите **"Add Environment Variable"**
4. Введите:
   - **Key:** `WEBHOOK_URL`
   - **Value:** `https://your-app-name.onrender.com` (ваш реальный URL)
5. Нажмите **"Save Changes"**

#### Шаг 6: Перезапустите сервис

Render автоматически перезапустит сервис после добавления переменной.

---

## ✅ Проверка

После установки `WEBHOOK_URL` проверьте:

### 1. Health Check

Откройте в браузере:
```
https://your-app-name.railway.app/health
```

Должен вернуться JSON:
```json
{
  "status": "healthy",
  "database": "connected",
  "llm": "available"
}
```

### 2. Логи

В логах должны быть строки:
```
✓ Webhook установлен: https://your-app-name.railway.app/webhook
✓ Webhook сервер запущен на порту XXXX
```

### 3. Тест бота

Отправьте боту в Telegram команду `/start` — он должен ответить!

---

## ⚠️ Важные моменты

1. **URL появляется ПОСЛЕ деплоя** — сначала задеплойте приложение, потом получите URL
2. **Без `/webhook`** — указывайте только базовый URL: `https://app.railway.app` (не `https://app.railway.app/webhook`)
3. **HTTPS обязателен** — Telegram требует HTTPS для webhook
4. **Обновите после изменения** — если Railway/Render изменил URL, обновите переменную `WEBHOOK_URL`

---

## 🔄 Что делать, если URL изменился?

Если Railway/Render изменил URL вашего приложения:

1. Скопируйте новый URL из настроек проекта
2. Обновите переменную `WEBHOOK_URL` в Railway/Render
3. Приложение автоматически перезапустится
4. Webhook автоматически обновится

---

## 📝 Примеры

### Railway:
```
WEBHOOK_URL=https://creativelandingbot-production.up.railway.app
```

### Render:
```
WEBHOOK_URL=https://creativelandingbot.onrender.com
```

---

## 🆘 Проблемы?

### URL не появляется

**Решение:**
- Убедитесь, что деплой завершился успешно
- Проверьте логи на наличие ошибок
- В Railway: проверьте вкладку "Networking"
- В Render: проверьте вкладку "Settings" → "Custom Domain"

### Webhook не устанавливается

**Решение:**
- Проверьте, что `WEBHOOK_URL` установлен в переменных окружения
- Убедитесь, что URL начинается с `https://`
- Проверьте логи на наличие ошибок
- Убедитесь, что приложение запущено (`webhook.py`)

### Health check не работает

**Решение:**
- Проверьте, что приложение запущено
- Проверьте логи на наличие ошибок
- Убедитесь, что база данных доступна
- Проверьте, что все переменные окружения установлены

---

## 🎉 Готово!

После установки `WEBHOOK_URL` ваш бот будет работать через webhook режим, что более эффективно для продакшн!

