# Python Telegram Bot (22.x поддерживает httpx 0.27–0.28; 20.7 требовал ~0.25.2)
python-telegram-bot==22.6

# LLM APIs
openai==1.6.0
anthropic==0.18.1
google-generativeai==0.3.2

# Database
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL driver

# Environment
python-dotenv==1.0.0

# HTTP requests (aiohttp 3.13.3: CVE-*; httpx 0.27.2: совместим с openai 1.6.0 — в 0.28 убран proxies)
aiohttp==3.13.3
httpx==0.27.2

# Utils
python-dateutil==2.8.2
tenacity==8.2.3  # Retry механизм для LLM API

# Security pins (транзитивные зависимости с известными CVE; pip-audit)
# protobuf не пиним: google-generativeai 0.3.2 требует 4.x; CVE-2026-0994 временно в ignore-vulns в CI
urllib3>=2.6.3
requests>=2.32.2
certifi>=2024.7.4

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0  # Coverage reports
pytest-mock==3.12.0  # Enhanced mocking
